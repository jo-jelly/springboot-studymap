{{>layout/header}}
<h1>멤버 구하기</h1>
<h1><a href="/studyGroup/save" role="button">글추가</a></h1>

<table class="table table-horiaontal table-bordered">
    <thead class="thead-strong">
    <tr>
        <th>게시글 번호</th>
        <th>제목</th>
        <th>작성자</th>
        <th>최종 수정일</th>
    </tr>
    </thead>
    <tbody id="tbody">
    {{#studyGroup}} <!--studyGroup라는 List를 순회 -->
    <tr>
        <td><a href="/studyGroup/view/{{id}}">{{id}}</a></td> <!--List에서 뽑아낸 객체의 필드를 사용한다. -->
        <td><a href="/studyGroup/update/{{id}}">{{title}}</a></td>
        <td>{{author}}</td>
        <td>{{modifiedDate}}</td>
    </tr>
    {{/studyGroup}}
    </tbody>
</table>

{{#studyGroup}}
<div class="card border-primary mb-12" style="max-width: 60rem;"> <!-- 파란색 테두리-->
    <div class="card-header">
        <a href="/studyGroup/update{{id}}">{{author}}</a>
        <p class="card-text">{{modifiedDate}}</p>
    </div>
    <div class="card-body">
        <h4 class="card-title"><a href="/studyGroup/view/{{id}}">{{title}}</a></h4>
        <p class="card-text">{{modifiedDate}}</p>
    </div>
</div>
{{/studyGroup}}
<div class="card border-light mb-3" style="max-width: 20rem;"><!-- 아무것도 없는 테두리-->
    <div class="card-header">Header</div>
    <div class="card-body">
        <h4 class="card-title">Light card title</h4>
        <p class="card-text">ㅠㅠㅠSome quick example text to build on the card title and make up the bulk of the card's content.</p>
    </div>
</div>

<div class="card border-light mb-3" style="max-width: 20rem;"><!-- 아무것도 없는 테두리-->
    <div class="card-header">Header</div>
    <div class="card-body">
        <h4 class="card-title">Light card title</h4>
        <p class="card-text">Some quick example text to build on the card title and make up the bulk of the card's content.</p>
    </div>
</div>

<div class="card border-light mb-3" style="max-width: 20rem;"><!-- 아무것도 없는 테두리-->
    <div class="card-header">Header</div>
    <div class="card-body">
        <h4 class="card-title">Light card title</h4>
        <p class="card-text">Some quick example text to build on the card title and make up the bulk of the card's content.</p>
    </div>
</div>
{{>layout/footer}}